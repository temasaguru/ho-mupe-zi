name: CI (テストとLighthouseスコアチェック)

on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # 将来的なVercelの対応を想定
        node: [16, 18]
    name: Node ${{ matrix.node }} で型チェックとテスト
    steps:
      - uses: actions/checkout@v3
      - name: Setup node
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node }}
          cache: 'yarn'
      - run: yarn install --frozen-lockfile
      - name: 型チェック
        run: yarn run typecheck
      - name: テスト
        run: yarn run test
  # lighthouse:
  #   # https://c-a-p-engineer.github.io/tech/2022/01/01/github-actions-lghthouse/
  #   # テストしてこのジョブを準備する間にVercelのデプロイができるはず
  #   needs: test
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@master
  #     - name: Lighthouse
  #       uses: foo-software/lighthouse-check-action@master
  #       id: lighthouseCheck
  #       with:
  #         urls: 'https://www.temasa.guru'
  #     - name: Lighthouseのスコアを確認する
  #       uses: foo-software/lighthouse-check-status-action@master
  #       with:
  #         lighthouseCheckResults: ${{ steps.lighthouseCheck.outputs.lighthouseCheckResults }}
  #         minPerformanceScore: '80'
  #         minAccessibilityScore: '90'
  #         minBestPracticesScore: '90'
  #         minSeoScore: '90'
  #     - if: failure()
  #       run: echo "しっかりしろ！なんだこの杜撰な製品は"
